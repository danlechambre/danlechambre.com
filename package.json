{
  "name": "danlechambre-com",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "wrangler types && astro dev",
    "dev-host": "wrangler types && astro dev --host",
    "build": "wrangler types && astro check && astro build",
    "preview": "wrangler types && wrangler pages dev ./dist"
  },
  "dependencies": {
    "@astrojs/mdx": "^4.0.8",
    "astro": "^5.1.9",
    "wrangler": "^3.107.2"
  },
  "devDependencies": {
    "prettier": "^3.4.2",
    "prettier-plugin-astro": "^0.14.1"
  }
}
